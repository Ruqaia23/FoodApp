import 'package:flutter/material.dart';

class DirectionsaRest extends StatelessWidget {
  const DirectionsaRest({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
